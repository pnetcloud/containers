# Generated by cloudnative-pg-timescaledb/scripts/generate-dockerfiles.sh
# Source entry: 18-bookworm
# Do not edit by hand; update cloudnative-pg-timescaledb/versions.yaml and run make generate.
FROM ghcr.io/cloudnative-pg/postgresql:18.4-standard-bookworm@sha256:6d1090d7ae1ea1cf2ba35959ba0fea38a78df91cf99e02e94a28b8e1f6df3de5

USER root

COPY scripts/verify-package-install.sh /usr/local/bin/verify-package-install

ARG TARGETARCH=amd64
RUN set -eux; \
    case "bookworm:${TARGETARCH}" in \
      trixie:amd64|trixie:arm64|bookworm:amd64|bookworm:arm64) ;; \
      *) echo "unsupported Debian variant or target architecture: bookworm:${TARGETARCH}" >&2; exit 1 ;; \
    esac; \
    . /etc/os-release; \
    test "${VERSION_CODENAME}" = "bookworm"; \
    test "$(dpkg --print-architecture)" = "${TARGETARCH}"; \
    export DEBIAN_FRONTEND=noninteractive; \
    apt-get update; \
    apt-get install -y --no-install-recommends ca-certificates curl gnupg; \
    install -d -m 0755 /usr/share/keyrings; \
    curl -fsSL https://packagecloud.io/timescale/timescaledb/gpgkey | gpg --dearmor -o /usr/share/keyrings/timescale-timescaledb.gpg; \
    echo "deb [signed-by=/usr/share/keyrings/timescale-timescaledb.gpg] https://packagecloud.io/timescale/timescaledb/debian/ bookworm main" > /etc/apt/sources.list.d/timescale-timescaledb.list; \
    apt-get update; \
    apt-get install -y --no-install-recommends \
      "timescaledb-2-postgresql-18=2.27.2~debian12-1804" \
      "timescaledb-toolkit-postgresql-18=1:1.23.0~debian12"; \
    /usr/local/bin/verify-package-install \
      "timescaledb-2-postgresql-18" "2.27.2~debian12-1804" \
      "timescaledb-toolkit-postgresql-18" "1:1.23.0~debian12"; \
    if [ "base" = "package" ]; then \
      apt-get install -y --no-install-recommends "postgresql-18-pgvector="; \
      dpkg-query -W -f='${Version}' "postgresql-18-pgvector" | grep -Fx ""; \
    else \
      test -f "/usr/share/postgresql/18/extension/vector.control"; \
      dpkg-query -S "/usr/share/postgresql/18/extension/vector.control"; \
    fi; \
    if [ "base" = "package" ]; then \
      apt-get install -y --no-install-recommends "postgresql-18-pgaudit="; \
      dpkg-query -W -f='${Version}' "postgresql-18-pgaudit" | grep -Fx ""; \
    else \
      test -f "/usr/share/postgresql/18/extension/pgaudit.control"; \
      dpkg-query -S "/usr/share/postgresql/18/extension/pgaudit.control"; \
    fi; \
    rm -rf /var/lib/apt/lists/* /tmp/*

LABEL org.opencontainers.image.source="https://github.com/pnetcloud/containers" \
      org.opencontainers.image.created="2026-06-09" \
      org.pnet.postgresql.major="18" \
      org.pnet.postgresql.version="18.4" \
      org.pnet.debian.variant="bookworm" \
      org.pnet.cnpg.tag="18.4-standard-bookworm" \
      org.pnet.cnpg.digest="sha256:6d1090d7ae1ea1cf2ba35959ba0fea38a78df91cf99e02e94a28b8e1f6df3de5" \
      org.pnet.timescaledb.version="2.27.2" \
      org.pnet.timescaledb_toolkit.version="1.23.0"

USER 26
