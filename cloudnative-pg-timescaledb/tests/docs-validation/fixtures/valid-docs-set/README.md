# Valid Docs Fixture

`latest` is convenience-only and points to PostgreSQL `18` on Debian `trixie`.

Prefer explicit immutable tags and digest-pinned references in CloudNativePG examples. Use the CloudNativePG Barman Cloud Plugin for backups; do not install legacy in-image `barman-cloud` tooling.
