name: Valid Allowlisted Permission
on:
  push:
permissions:
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      packages: write
    steps:
      - run: true
