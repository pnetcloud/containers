name: Disallowed Allowlisted Security Events Wrong Job
on:
  workflow_dispatch:
permissions: {}
jobs:
  evil:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - run: echo denied
