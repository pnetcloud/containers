name: Invalid Quoted Job Write Permission
on:
  push:
permissions:
  contents: read
jobs:
  "autocommit":
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: true
