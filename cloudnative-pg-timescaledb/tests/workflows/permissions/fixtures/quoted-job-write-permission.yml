name: Invalid Quoted Job Write Permission
on:
  push:
permissions:
  contents: read
jobs:
  "autocommit":
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - run: true
