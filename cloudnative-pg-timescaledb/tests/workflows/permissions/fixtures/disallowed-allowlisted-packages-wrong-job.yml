name: Disallowed Allowlisted Packages Wrong Job
on:
  workflow_dispatch:
permissions: {}
jobs:
  evil:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      packages: write
    steps:
      - run: echo denied
