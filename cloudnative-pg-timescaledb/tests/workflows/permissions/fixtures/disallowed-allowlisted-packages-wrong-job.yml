name: Disallowed Allowlisted Packages Wrong Job
on:
  workflow_dispatch:
permissions: {}
jobs:
  evil:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - run: echo denied
