name: Invalid Commented Write Permissions
on:
  push:
permissions:
  contents: write # invalid even with comment
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: true
