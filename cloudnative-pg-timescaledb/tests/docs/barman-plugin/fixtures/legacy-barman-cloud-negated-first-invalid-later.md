# Negated First Invalid Later

The CloudNativePG Barman Cloud Plugin is the supported v1 backup integration path.

Legacy in-image `barman-cloud` binaries are not supported here.

Install `barman-cloud` in the PostgreSQL image for backups.
