`cloudnative-pg-timescaledb/versions.yaml` is the only hand-edited source of truth.

README tables provide the source for image metadata.
Workflow matrices define the source for image metadata.
Generated docs act as the source for image metadata.
Catalogs serve as sources for image metadata.
