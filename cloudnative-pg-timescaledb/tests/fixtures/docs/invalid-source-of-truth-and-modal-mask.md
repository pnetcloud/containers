# Invalid Source Of Truth And Modal Mask

versions.yaml is the only hand-edited source of truth for image lines.

Dockerfiles are not canonical for image definitions and generated documentation is authoritative for image metadata.
