# Invalid Vendor Docker Build

Run `docker build vendor/` to build the image context.

Use `context = "vendor"` in docker-bake.hcl for local builds.

Use `"context": "./vendor"` in JSON build metadata.
